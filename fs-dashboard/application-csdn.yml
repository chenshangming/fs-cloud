spring:
  profiles:
    active: csdn
server:
  port: 8900
turbine:
  aggregator:
    clusterConfig: default
  appConfig: fs-consumer
  clusterNameExpression: "'default'"
spring:
  application:
    name: fs-dashboard
#management:
#  endpoints:
#    web:
#      exposure:
#        include: '*'